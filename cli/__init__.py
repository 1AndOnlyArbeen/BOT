"""Ultron CLI — the Claude-Code-style coder package.

Public surface:
    from cli.runner import run
    run(message, history) -> Iterator[event dict]

Everything else is implementation detail. See cli/README.md for the
architecture and the rationale behind each module.
"""
